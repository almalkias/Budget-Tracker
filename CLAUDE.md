# Budget Tracker — Project Instructions

## Language & Formatting

- When mixing Arabic and English text in a response, always put English words/terms on their own separate line so the text doesn't appear jumbled.

## Git

- Always push to branch `main` (`git push origin main`).
- Never push to any other branch unless explicitly asked.
